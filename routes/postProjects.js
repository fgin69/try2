const express = require('express');
const router = express.Router()

// route to controller for edit project by employer
// route to controller for add project by employer
// route to controller for delete project by employer
router.post('/edit',);
router.post('/add',);
router.post('/remove',); 



module.exports = router